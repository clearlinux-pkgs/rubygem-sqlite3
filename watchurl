# >=1, >=0, ~>1.3.3, >= 1.3.2, ~>1.3.2, 1.3.7
# The package stringex requires the version 1.3.7, but we already have the
# version 1.3.10, so we are gonna keet it. 
watchurl=https://rubygems.org/gems/sqlite3/versions/(1\.3\.7)
